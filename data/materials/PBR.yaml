# file: assets/materials/PBR.rmat

# The shader program this material uses. The path is relative to the asset root.
shader: assets/shaders/PBR.vert

# The parameters (uniforms) to be sent to the shader.
parameters:
  u_albedo_color: [1.0, 1.0, 1.0, 1.0]
  u_metallic: 0.8
  u_roughness: 0.3
  u_albedo_texture: assets/textures/metal_albedo.png